const state = {
  name: '前端开发者平台'
}

const getters = {
  name: state => state.name
}

const mutations = {}

const actions = {}


export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
